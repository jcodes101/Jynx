<div class="index-page">
  <div class="row">
    <p class="notice"> <%#= notice %> </p>

    <div class="column column-60">
      <div class="content-container">
        <div class="text-content">
          <h1>One link. Infinite possibilities.</h1>
          <p>Jynx lets you showcase everything you create, promote, and share — all from a single, powerful bio link. Perfect for creators, brands, and everyone in between.</p>

          <% if user_signed_in? %>
            <% if current_user.tree.present? %>
              <%= link_to "View my tree", current_user.tree, class: "button-1", target: "_blank" %>
            <% else %>
              <%= link_to "Create your own Jynx", new_tree_path, class: "button-1", target: "_blank" %>
            <% end %>
          <% else %>
            <%= link_to "Create your own Jynx", new_user_registration_path, class: "button-1", target: "_blank" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>